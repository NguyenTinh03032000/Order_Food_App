<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.order_food_master">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Order_Food_Master">
        <activity android:name=".FoodQuantityActivity"></activity>
        <activity
            android:name=".ListFood"
            android:label="Món ăn" />
        <activity android:name=".InsertFoodType" />
        <activity android:name=".InsertMenuFood" />
        <activity
            android:name=".MenuFood"
            android:label="Thực đơn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InsertDinTable" />
        <activity
            android:name=".DangKy"
            android:label="Đăng ký" />
        <activity
            android:name=".Login"
            android:label="Đăng nhập" />
        <activity
            android:name=".MainActivity"
            android:label="Trang chủ" />
    </application>

</manifest>